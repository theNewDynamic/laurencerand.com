const getCustomData = (entry) => {
  const { _type, title } = entry
  /*
  if(_type == "book") {
    return {
      ogTitle: "Book: " + title
    }
  }
  */
  return {}
}

export default getCustomData